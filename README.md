# AI-vs-Human-Art-Detector
AI vs Human Art Detector is a deep learning project that uses a Convolutional Neural Network (CNN) to classify images as AI-generated or human-created artwork. The model learns visual patterns from image data to support art authentication and ethical AI analysis
